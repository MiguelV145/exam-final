spring:
  application:
    name: final
  
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: CHANGE_ME_USE_A_LONG_RANDOM_SECRET_VALUE_32
  expiration: 3600000
